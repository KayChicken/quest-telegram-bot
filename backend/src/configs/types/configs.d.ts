export type TelegramBotConfigs = {
  API_KEY_BOT: string;
  API_ADMIN_KEY_BOT_ADMIN: string;
};

export type DatabaseConfigs = {
  DB_HOST: string;
  DB_PORT: number;
  DB_DATABASE: string;
  DB_USER: string;
  DB_PASSWORD: string;
};
